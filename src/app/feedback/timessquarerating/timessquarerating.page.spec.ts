import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TimessquareratingPage } from './timessquarerating.page';

describe('TimessquareratingPage', () => {
  let component: TimessquareratingPage;
  let fixture: ComponentFixture<TimessquareratingPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TimessquareratingPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
