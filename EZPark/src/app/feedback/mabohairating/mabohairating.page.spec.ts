import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MabohairatingPage } from './mabohairating.page';

describe('MabohairatingPage', () => {
  let component: MabohairatingPage;
  let fixture: ComponentFixture<MabohairatingPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MabohairatingPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
