import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AmanhillsratingPage } from './amanhillsrating.page';

describe('AmanhillsratingPage', () => {
  let component: AmanhillsratingPage;
  let fixture: ComponentFixture<AmanhillsratingPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AmanhillsratingPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
